using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Variables_globales : MonoBehaviour
{
    public static int n_victorias = 0;
    public static Vector3 playerposition = new Vector3(426, 9, 915);
    public static bool puzle_calamar = false;
    public static bool puzle_tuberia = false;
    public static bool puzle_jigsaw = false;
    // Start is called before the first frame update
    void Start()
    {
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
